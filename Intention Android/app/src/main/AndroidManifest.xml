<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- To check current running tasks/foreground app on newer Android versions -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <!-- Special-access permission (Settings.ACTION_USAGE_ACCESS_SETTINGS), not a
         runtime dialog — powers the per-app usage log for blocked apps. -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />
    <!-- Session check-in alarms. Auto-granted on API 31-32; denied by default on
         33+, where AlarmHelper falls back to an inexact alarm (the accessibility
         service's own expiry timer remains the precise cutoff). -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <!-- AlarmManager drops every pending alarm on reboot, so BootReceiver has to
         re-arm the check-ins for any session that is still running. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Draws the pass timer over whatever app the user is in (SessionOverlay).
         Special access, granted from Settings rather than a runtime dialog, and
         entirely optional: without it the timer degrades to the ongoing
         notification below, and blocking is unaffected either way. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- Only ever used for that fallback timer, so it is asked for at the point
         the overlay is declined rather than on first launch. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Backup stays on, but scoped: the blocklist and active sessions are
         never backed up, so a restore cannot be used to undo blocking, while
         the Play Billing account id (which keys the server-side credit
         balance) still survives a reinstall. See res/xml/backup_rules.xml. -->
    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Intention">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".CoachingActivity"
            android:exported="false"
            android:launchMode="singleInstance"
            android:theme="@style/Theme.Intention.Fullscreen"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:windowSoftInputMode="adjustResize" />

        <receiver
            android:name=".AlarmReceiver"
            android:exported="false" />

        <!-- "Finished" on the ongoing-pass notification. In the manifest rather
             than registered at runtime because a posted notification outlives
             the process that posted it. -->
        <receiver
            android:name=".SessionFinishReceiver"
            android:exported="false" />

        <!-- BOOT_COMPLETED and MY_PACKAGE_REPLACED are protected broadcasts only
             the system can send, so exporting the receiver doesn't widen who can
             reach it. -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <service
            android:name=".IntentionAccessibilityService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

    </application>

</manifest>
