{
  "name": "intention-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Intention's hosted coach backend: verifies App Store / Play subscriptions and proxies coaching calls under Intention's own LLM key.",
  "engines": {
    "node": ">=20.12.0"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env src/index.js",
    "dev": "NODE_ENV=development node --env-file-if-exists=.env --watch src/index.js"
  }
}
